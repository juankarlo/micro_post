<%= semantic_form_for(current_user.relationships.find_by(followed_id: @user
.id),html: { method: :delete }, remote: true) do |f| %>

    <%= f.action :submit,label: "Unfollow", :button_html => {:class => "btn
    btn-xs btn-info"} %>
<% end %>

<%#= form_for(current_user.relationships.find_by(followed_id: @user.id),
             html: { method: :delete }) do |f| %>
    <%#= f.submit "Unfollow", class: "btn btn-xs btn-info" %>
<%# end %>